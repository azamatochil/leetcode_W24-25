def divide_and_conquer(S):
    print('AAAA')
    #(1). Divide the problem into a set of sub problems
    #[S1, S2, ..., Sn] = divide(S)

    # (2). Solve the sub problems recursively,
    # obtain the results of sub problems as [S1, S2, ... Sn].
    #rets = [divide_and_conquer(Si)] for Si in [S1, S2, ..., Sn]]
    #[R1, R2, ..., Rn] = rets

    # (3). combine the results from the sub problems,
    # and return the combined result
    # return combine([R1, R2,..., Rn])
